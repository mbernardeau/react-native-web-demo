import React from 'react';
import ReactNative, {
  View,
  Text,
} from 'react-native';
import Viewer from './Viewer';

export default () => (
  <View>
    <Text>Welcome into the App</Text>
    <Viewer />
  </View>
);
